Place the provided logo image file into this project folder and name it `logo.png`.

Where to put it:
- Path: `c:\Users\vishal\Desktop\FireCompass\STB\logo.png`

Why:
- Both `openredirect_web.html` and `login.html` reference `logo.png` in the same directory for the navbar/heading.

Recommended size / format:
- PNG (already provided). Recommended height: 28-40px for navbar/heading. The pages scale via CSS `height` on the `<img>` tag.

If you prefer hosting the image remotely:
- Replace `logo.png` in the `<img src="...">` with an absolute URL (e.g., `https://example.com/logo.png`).

If you'd like, I can:
- Resize/optimise the image and add it to the repo for you.
- Change placement or spacing of the logo on either page.
